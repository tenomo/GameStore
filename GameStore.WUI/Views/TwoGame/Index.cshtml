@model IEnumerable<GameStore.WUI.Models.Game>

@{
    ViewBag.Title = "List";
}
@foreach (var p in @Model)
{
    <div>
        <h3>@p.Name</h3>
        <p>@p.Description</p>
        <h4>@p.Price.ToString("# руб")</h4>
    </div>
}

